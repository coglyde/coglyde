import { AddOnsSection } from "./AddOnsSection";

// The automations category is the full add-on menu (monthly only, so the
// billing toggle is hidden for this category).
export function AutomationsPanel() {
  return <AddOnsSection />;
}
