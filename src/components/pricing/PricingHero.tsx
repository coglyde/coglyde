export function PricingHero() {
  return (
    <section className="relative z-10 px-6 pt-32 pb-6 sm:px-10 sm:pt-40">
      <div className="mx-auto flex max-w-[60rem] flex-col items-center text-center">
        <p className="text-xs font-medium uppercase tracking-[0.22em] text-violet-400">
          Pricing
        </p>
        <h1 className="mt-4 max-w-[50rem] text-balance text-[2.6rem] font-medium leading-[1.08] tracking-tight text-white sm:text-[3.2rem] md:text-[3.8rem]">
          A build that fits, then plans that grow with you
        </h1>
        <p className="mt-5 max-w-[42rem] text-pretty text-base leading-[1.55] text-white/60 sm:text-lg">
          Your site is custom-coded and quoted to its complexity. After launch,
          keep it sharp with simple monthly plans for care, search and
          automation. No lock-in, cancel anytime.
        </p>
      </div>
    </section>
  );
}
